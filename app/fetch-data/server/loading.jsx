const loading = () => {
    return (
      <div className="loader" style={{backgroundColor: "goldenrod"}}>
        loading data from server side
      </div>
    );
  };
  
  export default loading;
  